# Project-Backtest_web-app-preview
AI strategy backtesting platform: user describes a trading strategy in plain English → LLM translates to a constrained schema → deterministic recap confirmed by user → wrapped `backtesting.py` engine runs it on free market data → metrics passthrough + chart + LLM interpretation
